- 采用 IIFE (立即调用函数表达式) 封装所有 JS 模块，避免全局变量污染，仅通过 `window` 对象暴露必要的全局配置或插件接口。
- 遵循“配置与逻辑分离”原则，将图层元数据（名称、文件路径、分组）统一定义在 `geo-config.js` 中，业务逻辑脚本不硬编码数据路径。
- 实施“懒加载与状态持久化”模式，地图控件（如比例尺、编辑工具）根据用户偏好动态创建/销毁，并将状态同步至 `localStorage`。
- 采用“多级回退”加载策略，优先从 IndexedDB 缓存读取数据，其次尝试本地相对路径，最后回退至 COS 远程地址，确保高可用性。