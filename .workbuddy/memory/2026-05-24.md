# 2026-05-24

## 线/面标签显示功能
- 去掉 `rebuildLoadedPointLayers` 中 `if (!isPoint) return;`，线/面图层响应标签/聚类开关
- 聚类更新逻辑包在 `if (isPoint)` 内，标签影响所有图层
- `onEachFeature` 中线/面要素加 `bindTooltip(name, { permanent: false, direction: 'center' })`
- 修复 `labelToggle` HTML 默认 `checked` 与 JS `labelEnabled = false` 不一致

## 投点编辑器优化
- **粘贴框固定尺寸**：`pdPasteArea` 宽 `252px`、高 `48px`、`resize:none`
- **全局粘贴 + 剪贴板自动读取**：`paste` 事件拦截非输入框粘贴，`visibilitychange` + `focus` 主动读取剪贴板
- **重复内容跳过**：`_lastClipText` 变量，paste 和 `tryReadClipboard` 两个入口均检查，相同内容不重复触发
- **自动展开侧边栏 + 滚动到编辑器 + Toast 提示**
- **表格列宽动态计算**：按列名字符数+1ch，`min(60px, Nch)`，最小 4ch，`table-layout:fixed`

## 状态圆点点击下载 GeoJSON
- 绿色（loaded）状态圆点支持点击下载对应 GeoJSON 文件
- CSS：加 `cursor:pointer` + hover `scale(1.4)` 效果，title 改为"已加载（点击下载）"
- 预置图层：优先从 `searchIndexMap` 缓存取完整 features（避免大数据集重新 fetch），回退到 `GzIdbLoader.fetch`
- 用户上传图层（含投点生成图层）：直接从 `userLayerGeoJson[uid]` 取原始数据
- 新增函数：`downloadLayerGeoJson(cbId, filePath, layerName)` + `triggerDownload(geoJson, fileName)`
