# 2026-06-19 Daily Log

## CSS 修复：Dialog MD 断行 + 要素加载不可选中

- **dialog.css**:
  - `.app-dialog .dialog-body`: 添加 `overflow-x: hidden`, `word-break: break-word`, `overflow-wrap: break-word`，防止长段英文/无空格文本导致横向滚动
  - `.app-dialog .dialog-body th, td`: 同上，表格内文本也加断词
  - `.app-dialog .dialog-body pre`: `white-space: pre-wrap` + 断词，代码块内长文本自动换行
  - `.app-dialog .dialog-body p`: `text-indent: 2em`（首行缩进两格）
  - `.app-dialog .dialog-body li`: `text-indent: 2em; list-style-position: inside`（首行缩进含圆点/数字）
- **main.css**:
  - `.layer-trigger`: 添加 `user-select: none`，让"要素加载"四个字不可选中

## MD 帮助文档 + 问号图标

- 创建 `docs/static-vector-help.md`：静态矢量要素使用说明 + 全部数据来源表格
- 创建 `docs/local-layer-help.md`：本地图层使用说明，含拖拽/上传/PWA打开/投点说明
- **geojsonloader.js**: 两个面板的 `<summary>` 内 selectAll checkbox 后各加一个绿色圆形 `?` 图标
  - 点击 `?` 调用 `showMarkdown()` 打开对应帮助文档
  - `e.stopPropagation()` 防止触发 summary 折叠/展开
  - hover 加深色效

## 侧边栏改用纯 CSS 开关（Checkbox Hack）

- **index.html**:
  - 新增 `<input type="checkbox" id="sidebarToggle" hidden>` 控制开关
  - `.layer-trigger` 从 `<div>` 改为 `<label for="sidebarToggle">`
  - 新增 `<label class="sidebar-backdrop" for="sidebarToggle">` 点击外部关闭
  - `aboutLink` 从 `.about-link` 改为 `.help-icon.help-icon-lg`，与图层组问号图标共用样式
- **main.css**:
  - `.layer-panel.active` → `#sidebarToggle:checked ~ .layer-panel` 纯 CSS 控制
  - 新增 `.sidebar-backdrop` 遮罩样式
  - 删除 `.about-link` 独立样式块，改用 `.help-icon` + `.help-icon-lg` 修饰符
  - `.search-bar` 添加 `padding: 0 8px` 与其他区块水平对齐
- **geojsonloader.js**:
  - 删除约 30 行 JS 面板开关代码（toggle / click-outside / stopPropagation）
  - `layerPanel.classList.add("active")` 改为 `sidebarToggle.checked = true`
  - 删除多余的 `__yugis_justResized` 防抖相关死代码

## 布局重构：模块化面板系统 + 排序优化

- **index.html**:
  - 搜索栏移至最顶部（地图设置之前）
  - 新增 `.panel-module` 统一类，标记所有侧边栏内部组件为模块
  - 标题栏 → `.panel-title` class，`?` 和 📌 用 `.title-right` + `margin-left: auto` 右对齐
  - `style="flex:1;"` 改为 `.summary-title` class
  - `style="display:none"` 改为 `.file-input-hidden` class
- **main.css**:
  - 新增 `.panel-module` 基类：`margin-bottom: 6px`，所有模块统一间距
  - 新增 `.panel-title` / `.title-right` / `.summary-title` / `.file-input-hidden`
  - 删除所有 `.about-link` 残余样式（已被 `.help-icon` 取代）
  - `.toggle-section` / `.layer-section` 移除单独 margin-bottom，由 `.panel-module` 统一管理
- **geojsonloader.js**: 无改动（样式已全部移至 CSS）

## 帮助图标统一为 `<span>` + 声明式 data-dialog 绑定

- **index.html**:
  - `#aboutLink` 从 `<a>` 改为 `<span class="help-icon help-icon-lg">`，与图层组 `?` 图标标签统一
  - 三个帮助图标均添加 `tabindex="0" role="button"`（无障碍）
  - 添加 `data-dialog="path/to/file.md"` 和 `data-dialog-title="..."` 属性
- **dialog.js**:
  - 新增 `DOMContentLoaded` 自动绑定：扫描 `[data-dialog]` 元素，点击时调用 `showMarkdown()`
  - 自动处理 `e.stopPropagation()`（适合 `<summary>` 内部使用）
- **geojsonloader.js**:
  - 删除约 30 行手动 help icon 事件绑定（aboutLink / dataHelpIcon / localHelpIcon 共 3 处）
  - 全部由 HTML 的 `data-dialog` 属性声明式替代

## 细节优化与 BUG 修复

- **app.js**: 地图设置中"编辑测量"和"图层控件"顺序互换（编辑测量在上）
- **细节持久化**: 新增 `initDetailsPersistence()`，`[data-persist-details]` 属性控制 `<details>` 开合状态自动保存/恢复
- **搜索图层持久化**: 搜索点击勾选的图层现在调用 `persistLayerCheckState()`，刷新后恢复
- **展开面板**: 新增 `expandToLayerGroup(cb/i)` 抽象函数，在 checkbox change handler / restore / addUserLayer 中统一调用
- **Ctrl+F 修复**: 改用 `sidebarToggle.checked = true` 替代已删除的 `classList.add("active")`
- **app.js**: 删除旧的 toggleSection 持久化 + 自动折叠逻辑
- **localStorage key 统一**: `yugis_sidebar_pinned`→`dupal_sidebar_pinned`，`yugis_panel_width`→`dupal_panel_width`，`clusterEnabled`→`dupal_cluster_enabled`，`labelEnabled`→`dupal_label_enabled`
- **main.css**: 恢复 `.layer-item input[type="checkbox"]` 自定义 appearance，用 `--layer-color` 显示各图层专属颜色而非统一绿色

## CSS 变量改造 + 深色模式重设计 + pointdrop 样式分离

- **main.css / dialog.css**: 全面引入 CSS 变量体系（`--panel-bg`、`--section-*`、`--text-*` 等约 40 个变量）
  - 日间/深色模式只靠修改变量值，无需重复写选择器
  - 新增 `[data-theme="light"]` 和 `[data-theme="dark"]` 属性选择器，开关双向可控
  - 深色模式重新配色：`#1c1c20`（面板背景）、`#252529`（模块背景）、文字 `#e0e0e0`，更柔和专业
- **main.css**: 修复约 30 处硬编码颜色改用 `var(--xxx)`（状态灯、搜索框、折叠区、按钮、版本号等）
- **geojsonloader.css**: 修复 `layer-color-btn`、`color-modal-close` 使用变量
- **pointdrop.js → pointdrop.css**: 完全移除 pointdrop.js 中约 20 处 `style.cssText` 内联样式
  - 新建 `assets/pointdrop.css`，提取所有投点编辑器样式为 `.pd-*` class
  - `index.html` 引入 pointdrop.css
  - 删除 geojsonloader.css 中约 50 行旧的投点样式

## 弹窗样式统一归集到 dialog.css

- **dialog.css**: 从 `main.css` 移入 `.leaflet-popup-content-wrapper`、`.leaflet-popup-tip`、`.leaflet-popup-close-button`、`.hover-tooltip`、`.hover-popup`（约 60 行）
- **dialog.css**: 从 `geojsonloader.css` 移入 `.feature-popup`、`.feature-popup-title`（约 40 行）
- **main.css**: 删除上述移动的样式
- **geojsonloader.css**: 删除 feature-popup 相关样式
- dialog.css 现为所有弹窗/模态/Toast/要素弹窗/提示卡/Leaflet Popup 的唯一样式文件（~120 行弹窗专属样式）

## color-scheme + 确认框修复

- **main.css**: `html { color-scheme: light dark }` → `color-scheme: light`（由 JS 动态控制）
- **app.js**: `enable()` 设置 `style.colorScheme = "dark"`，`disable()` 设置 `style.colorScheme = "light"`，浏览器原生控件（滚动条、选择框等）随之切换
- **dialog.css**: 确认框按钮 `.confirm-cancel`、`.confirm-ok` 从硬编码色改为 `var(--dlg-cancel-bg)`、`var(--dlg-ok-bg)` 等变量，深色模式下正确切换

## 杂项

- `overview.md` 移至 `docs/overview.md`

## 天地图地名搜索集成

- `index.html`: `window.TDT_TK = ...` 暴露天地图 key 到全局
- `geojsonloader.js`: 新增 `tiandituSearch()` 调用天地图 API V2 地名搜索（queryType=7），400ms 防抖 + 请求结果缓存
- 搜索结果中新增 `🗺️ 地名` 标签项，点击直接飞至该地点
- 更换自带 tooltip 为原生 `title`，删除 60 行自定义 tooltip JS + 30 行 CSS

## 版本号更新

- `service-worker.js`: v1.8.3 → v1.8.4，`pointdrop.css` 加入缓存列表
- `docs/CHANGELOG.md`: 写入当日完整变更记录
