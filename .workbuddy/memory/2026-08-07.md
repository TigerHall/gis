# 2026-08-07 工作日志

## 图层不可选中 + 弹窗按钮外移 + 用户图层永久持久化（重新应用 + Bug 修复）

Git 库损坏导致代码回退，全部重新应用，并修复两个 Bug。

### 改动文件

- `assets/geo-config.js` — 浙江适飞区添加 `selectable: false`
- `assets/geojsonloader.js` — `layerSelectableMap`、7条点击路径全覆盖、设置弹窗「可选中」开关、用户图层持久化去掉 isRememberLayerEnabled 守卫、clearAllLayerStates 不再删除用户数据
- `index.html` — popup 按钮移到 `.leaflet-popup` 外层右侧竖向排列
- `assets/main.css` — `.popup-ext-btn-wrap` + `.popup-ext-btn` + `.leaflet-popup{overflow:visible!important}`
- `docs/CHANGELOG.md` — 更新记录

### Bug 修复

1. **`clearAllLayerStates()` 删除用户图层**：选「不恢复」时不再删除 IDB 缓存和 metadata，只清除内置图层勾选状态
2. **selectable 漏网之鱼**：DOM cluster dblclick、线/面 block pointToLayer popup、线/面 onEachFeature dblclick 三条路径补充 selectable 检查
3. **用户图层自动加载**：`restoreUserLayers()` 从无条件调用移入 confirm 弹窗「恢复」回调内，只有用户点「恢复」时才加载

## 用户图层存储逻辑终版梳理（重头简化）

用户要求：上传后只有 × 才删；重新打开默认不勾选；点「恢复」才勾选原本打开的图层。

- `restoreUserLayers` 的 `wasChecked` 去掉 `isRememberLayerEnabled` 纠缠，严格按 `dupal_user_layer_<id>` 还原（true→勾选加载，false/未保存→不勾选）
- `addUserLayer` 上传时主动 `setItem("dupal_user_layer_<id>", String(checkbox.checked))`，确保「恢复」能读到初始打开状态
- 确认全仓删除路径：仅 `geojsonloader.js:3481-3483`（× 按钮）真正删用户数据；`clearAllLayerStates` 与 app.js 开关均排除 `dupal_user_layers` 和 IDB（前缀碰撞已修）

