# 2026-05-01 工作记录

## PWA 图标配置更新
- 更新 `manifest.json`：图标路径改为 `assets/images/icon/android/launchericon-*.png`，截图改为 `.jpg`
- 更新 `index.html`：添加 iOS `apple-touch-icon` 链接（SVG优先 + PNG降级）
- 新建 `browserconfig.xml`：Windows 瓷砖图标配置
- SVG 图标优先使用（`icon.svg`）

## 代码合并
- 将 `app.js`（Service Worker 注册代码，仅14行）合并到 `index.html` 中
- 删除了独立的 `app.js` 文件

## 添加 GeoJSON 文件关联打开功能
- 更新 `manifest.json`：添加 `file_handlers` 字段，支持 `.geojson` 文件关联
- 更新 `index.html`：
  - 添加 File Handling API 支持（`launchQueue.consume`）
  - 添加拖放支持（drag & drop）
  - 实现 `loadExternalGeoJson()` 函数，加载外部 GeoJSON 到地图
- 更新 `service-worker.js`：版本号改为 `v1.4.0`，移除 `app.js` 缓存，添加 `browserconfig.xml` 缓存
- 更新 `README.md`：添加文件打开功能说明
