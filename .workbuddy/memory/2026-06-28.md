# 2026-06-28 工作日志

## 热点专题图层组

- 新增 `geo-config.js` 图层组 "热点专题"，包含 `wc2026_48_teams` 和 `wc2026_round32_teams` 两个世界杯地图

## 标签系统完善（geojsonloader.js）

1. **`_getFeatureName`** 新增第一字段值 fallback：`Name → name → NAME → 第一个非空字段值`
2. **新增 `_getLabelText`** 点要素标签提取函数，支持同样的 fallback 链（取代原来三处 `feature.properties[labelField] || null`）
3. **新增 `_getFirstFieldValue`** 工具函数，遍历 properties 返回第一个非空值
4. **面/线要素标签改为永久显示**：`_bindHoverLabel` → `_bindPermanentLabel`，在 `onEachFeature` 中直接绑定 `permanent: true` 的 tooltip，替代原先 mouseover 动态绑定的方式。由 `labelEnabled` 全局开关控制

## 标签字段统一到 geo-config.js

- 移除 `STATION_LABEL_CONFIG` 配置表（geo-utils.js）和 `geojsonloader.js` 中的引用
- 所有图层自定义标签字段统一写在 `geo-config.js` 的 `labelField` 属性中
- 现有迁移：DSDP→Hole、ODP→Fullname、IODP13-26→site、volcanos→NAME、hotspots→geodesc、hydrothermal*vents→Name ID、wc2026*\*→name_zh
- fallback 简化为：`labelFieldMap[checkboxId] || "Name"`
