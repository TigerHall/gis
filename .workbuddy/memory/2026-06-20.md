# 2026-06-20

## 付费高级功能激活系统

- **`assets/codes.json`** → **内联到 `assets/app.js`**: 激活码移入 `_PR_CODES` 数组（代码顶部，方便每月更换），删除 codes.json
- **`assets/app.js`**: premium 模块使用 `_PR_CODES.indexOf()` 直接验证，不再 fetch
- **`assets/premium.js` → 合并到 `assets/app.js`**: 激活码验证模块移入 app.js，删除 premium.js
- **`assets/dialog.css`**: 新增 `.premium-dialog` / `.premium-input` / `.premium-btn` 等 CSS 类（替代原 JS 内联样式）
- **`index.html`**: 移除 premium.js 引用
- **`app.js`**: TOGGLE_GROUPS 新增 "高级" 分类 + "高级功能" 开关，toggleConfig 新增 `premiumToggle`，开启时弹激活码弹窗
- **`geojsonloader.js`**: 两处下载 GeoJSON 的 click handler 新增 `window.premiumCheck()` 判断，未激活时Toast提示
- **`service-worker.js`**: 移除 premium.js（已合并），保持 codes.json

## 激活体验优化

- **`app.js`**: 激活成功后弹窗切换为 QR 码（`?activate=CODE` 网址），手机扫码自动激活；URL 参数 `?activate=xxx` 自动检测激活
- **`app.js`**: 已激活时页面加载自动同步高级功能开关为开启状态
- **`app.js`**: `doRefresh()` 保留 `ogv_premium_active` 不被 `localStorage.clear()` 清除
- **`README.md`**: 在"联系我们"后新增"高级功能"章节，内置测激活码 `837291`
- **激活弹窗提示**: 去除首行缩进，底部提示激活永久有效

## Toast 按钮样式修复

- **`dialog.css`**: 补回 `.toast-cnt` / `.toast-msg` / `.toast-action` / `.toast-close-btn` 样式（CSS 重构时遗漏）

## 地图设置重构

- **`app.js`**: TOGGLE_GROUPS 新增 `type: "button"` 支持，新增"操作"分类 + `📷 导出图片` 按钮
- **`app.js`**: 深色模式开关从"显示"移入"高级"分类
- **`main.css`**: 新增 `.action-btn` 按钮样式（flex 自适应，方便后续加按钮）

## MD 文档缩进改进

- **`dialog.css`**: `text-indent: 2em` 从 `.app-dialog .dialog-body p` 改为 `#_mdBody > p`，仅 MD 文档直接段落缩进，blockquote 内不缩进

## 帮助文档更新

- **`docs/static-vector-help.md`**: 下载 GeoJSON 改为高级功能描述
- **`docs/local-layer-help.md`**: 新增"高级功能：下载 GeoJSON"章节，说明格式转换用途

## 地名搜索结果积累图层

- **`geojsonloader.js`**: 新增 `addTdtPointToLayer(poi)` 函数 + `_tdtSearchPoints` 积累数组
- 点击地名搜索结果时，创建/追加到"🔍 地名搜索结果"用户图层（使用 `addUserLayer` 完整生命周期）
- POI 全部 API 字段（name/address/phone/poiType/source/hotPointID/经纬度）写入属性表，`Name` 排第一作为弹窗标题
- 搜索结果列表显示名称 + 地址 + 电话，title 悬浮显示全部字段
- **`service-worker.js`**: v1.8.5 → v1.8.6

## GPS 定位 + 位置记录

- **`app.js`**: TOGGLE_GROUPS "操作" 新增 "📍 定位当前位置" 按钮
- **`geojsonloader.js`**: 新增 `window.startGeoLocate()` + `addGeoPointToLayer()` 函数
- 点击定位按钮 → 浏览器请求权限 → 获取 GPS 坐标 → 弹窗输入 备注/分类 → 追加到"📍 我的记录"积累图层
- 定位失败时 Toast 提示具体原因（权限拒绝/无信号/超时）

## 图层会话化 + 持久化修复

- **`geojsonloader.js`**: 地名记录和位置记录改用会话时间戳命名（`🔍 地名记录 2026/6/20 11:18` / `📍 位置记录 2026/6/20 11:19`），每次刷新生成新图层
- **`geojsonloader.js`**: 修复持久化—`addUserLayer` 在有 `existingPersistentId` 时跳过 IDB 保存，改为手动 `setCache` + `saveUserLayerMeta`
- **`geojsonloader.js`**: 删除旧图层时清理 DOM checkbox + layerCache + userLayerGeoJson（避免刷新后恢复提示但无数据）

## 通用要素计数

- **`geojsonloader.js`**: 新增 `updateLayerCount(checkboxId, features)` 函数，在所有图层搜索索引构建完成后自动追加 `(N 点/线/面/要素)` 计数
- 删除了 TDT/Geo 图层自定义 `.tdt-count` / `.geo-count` badge，统一走通用机制
- 修复 `updateLayerCount` 因函数定义位置靠后导致 IDB 缓存恢复路径报 `not defined` 的 bug（提前到 `waitForMap` 开头）

## 静态矢量帮助文档改写

- **`docs/static-vector-help.md`**: 7 张数据来源表格改为纯文本列表描述，去除 `local-layer-help.md` 中多余表格
- **`MEMORY.md`**: 新增 MD 文档约定（尽量不用表格）
