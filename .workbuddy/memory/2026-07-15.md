# 2026-07-15

## GZ 文件解析支持

- `assets/file-handler.js` 新增 `.gz` 格式解析支持，覆盖所有入口：
  - **单文件**：`loadFileAsUserLayer` 新增 `.gz` 分支，用浏览器原生 `DecompressionStream("gzip")` 解压后自动识别内部 .geojson/.json/.kml
  - **ZIP/KMZ 内部**：循环识别 `.gz` 条目 → `geojsonGzFiles` / `kmlGzFiles` → 解压后投递
  - **文件夹扫描**：`"gz"` 加入 `nonShpExts`，`.gz` 文件自动回调 `loadFileAsUserLayer`
  - **拖放**：正则中补充 `gz`，非文件夹拖放也支持 `.gz`
  - 所有提示信息同步更新
- 新增工具函数 `decompressGz(arrayBuffer)`，封装 `Blob.stream().pipeThrough(DecompressionStream)` + `new Response().arrayBuffer()` 链

## PWA 关联打开 .gz

- `manifest.json`：`file_handlers.accept` 新增 `"application/gzip": [".gz"]`
- `index.html`：文件选择器 `accept` 新增 `.gz,application/gzip`
- 配合 `file-handler.js` 已有的 `launchQueue` + `loadFileAsUserLayer` 逻辑，`.gz` 文件被 OS 唤起 PWA 时自动解压并加载

## 新增三个勘探合同区面图层

### geo-config.js
- "海底矿产资源"组新增三个面图层：
  - **PMN**（`01_pmn_exploration_areas.geojson.gz`）全球多金属结核勘探合同区
  - **PMS**（`02_pms_exploration_areas.geojson.gz`）多金属硫化物勘探合同区
  - **CFC**（`03_cfc_exploration_areas.geojson.gz`）富钴结壳勘探合同区
- 标注字段 `labelField: "Contractor"`
- 默认内部多颜色：`colorMode: "field"`, `colorField: "Contractor"`

### geojsonloader.js
- 新增 `defaultColorModeMap` / `defaultColorFieldMap` 存储 config 默认值
- UI 构建时（`buildGeoJsonLayerGroup` 外部）存储 config 默认值
- `buildGeoJsonLayerGroup` 中回退链条：savedSettings → config 默认值 → 代码默认值
- `getColorPanelHTML` 中同样回退到 `defaultColorModeMap`，修复对话框打开时未加载图层不显示 field 模式的问题

## geo-config.js 注释补充

- 图层配置选项表格新增 `colorMode` / `colorField` 字段说明，含取值和示例
- 顶部"完整选项"示例同步更新，新增按 Contractor 分色的完整样例

## 新增海底光缆图层

- "海底基础信息"组新增两个图层：
  - **海底光缆 Submarine Cables**（`TeleGeography_Cables.geojson.gz`，704 条线要素，青色 `#00ACC1`）
  - **光缆登陆点 Landing Points**（`TeleGeography_LandingPoints.geojson.gz`，1900 个点要素，蓝绿色 `#00897B`）
  - 标注字段均为 `name`

## 新增 EBSA 海洋保护区

- "海底矿产资源"组新增：
  - **EBSA 海洋保护区&特殊生态区**（`EBSAs_4326_Vis.geojson.gz`，299 个面要素，绿色 `#66BB6A`，colorMode: "single"）
  - 标注字段均为 `name`
