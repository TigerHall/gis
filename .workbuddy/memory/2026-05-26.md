# 2026-05-26 工作日志

## 导出地图图片功能
- 用 html2canvas 替换旧的纯 Canvas 合成方案
- 所有瓦片图层加 `crossOrigin: 'anonymous'`，ArcGIS URL 从 `//` 改为 `https://`，去掉 `allowTaint: true`
- 绑定 Ctrl+E 快捷键触发导出

## 版本号 CSS
- #appVersion 加 `margin-bottom: -8px`（后改 -12px）贴近面板底部

## 备案号 + GitHub Actions
- 备案号 `蜀ICP备2025119436号-2` 加在 `#map` 下方，CSS 固定右下角 `position:fixed; bottom:6px; right:8px; z-index:1100`
- 新建 `.github/workflows/deploy-cos.yml`，push main 时同步整个仓库到腾讯云 COS
- 需要用户在 GitHub 仓库 Secrets 中填写：COS_SECRET_ID / COS_SECRET_KEY / COS_BUCKET / COS_REGION

## About 页面 + 侧边栏入口
- 新建 `about.html`，包含功能概览、全部数据来源（7个分组40+图层）、技术栈说明
- 侧边栏 `<h3>图层要素</h3>` 右侧加 ℹ️ 圆形按钮（`.about-link`），hover 变蓝，title="关于本站"
- `.layer-panel h3` 改为 `display:flex; align-items:center; gap:6px` 布局图标

## About 页面重构
- 移除错误引用的 `assets/main.css`（地图主页样式会覆盖about自己的样式）
- 主题色从蓝色改为 `#9c9` 绿色系（h2/链接/tag 统一）
- 图层表格改为 `<ul><li>` 列表格式，方便后续增删
- 重构为经典 头/身/脚 布局：fixed header（返回地图+标题）、scrollable content + 右侧自动生成 TOC 目录、fixed footer（备案号）
- TOC 由 JS 从 h2/h3 自动生成，带 scroll spy 高亮当前章节
- 移动端（≤768px）隐藏 TOC 侧边栏
