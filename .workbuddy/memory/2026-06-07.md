# 2026-06-07

## 底图控件优化

- 新增「更多底图」开关（地图设置内），默认关闭，控制 ArcGIS 扩展底图的显示
- 新增「天地图地名标注」多选叠加层
- 重构底图控件为动态重建机制（`rebuildLayerCtrl`）
- 默认底图改为 ArcGIS-海洋底图
- 默认状态下单选图层精简为 9 个底图

## 天地图图层精简

- 从 imgLayers/vecLayers/terLayers 中移除内置标注层，改为纯底图
- 删除冗余的「天地图纯影像」条目（与无标注的「天地图影像底图」重复）
- 删除废弃的 tdtImgAnno/tdtImgLayer2/tdtVecAnno/tdtTerAnno 变量
- 图层控件中「OSM底图_边界有误慎用」更名为「OpenStreetMap」
- 标注统一由多选图层「天地图地名标注」独立控制

## GeoJSON 路径优化

- 去除域名分支判断逻辑
- 统一始终优先走相对路径（`./assets/geojson/`），失败回退 COS 直连
- dupal.cn 本身就是 COS 静态域名，相对路径即 COS 路径且走 CDN 加速

## 侧边栏搜索增强

- 「图层要素」重命名为「静态矢量要素」，明确与未来动态要素区分
- 搜索框新增图层名和图层组名的模糊搜索
- 搜索图层命中后点击自动勾选并缩放到图层范围
- 搜索占位提示改为「搜索图层名 / 要素属性」
- searchRegistry 增加 groupName 字段

## 侧边栏面板视觉美化

- 重写 `.layer-section-content`：添加 flex 布局 + gap 间距，替代无间距默认块级显示
- 重写 `details.layer-group` 卡片化设计：添加圆角、边框、hover/展开时阴影过渡
- 图层组 summary 改为 `#f2f8f2` 背景 + 展开时分割线
- 添加 `.layer-group-children` 左侧装饰竖线（2px 渐变线），增强层次
- `.layer-plain` 无分组容器添加卡片边框
- `.layer-item` 添加 `:active` 和 `:has(:checked)` 选中态（左侧绿条指示）
- 箭头 `.layer-group-arrow` 添加透明度过渡，展开时变亮
- 暗色模式全线适配以上改动
- 清理 geojsonloader.css 中与 main.css 冲突的冗余样式

## 搜索功能增强：要素属性搜索 + 结果区分

- 去除要素搜索仅限已勾选图层的限制，只要有索引数据（曾加载过）即可搜索要素属性
- 搜索结果添加 emoji 前缀：📁 表示图层/组名匹配，📍 表示要素属性匹配
- 搜索结果点击要素时，若图层未加载自动先加载再高亮定位
- 新增 `_loadedCallbacks` 回调队列 + `fireLoadedCallback` 触发机制，支持异步加载后自动定位
- 提取 `highlightAndLocateFeature` 为独立函数，消除代码冗余

## 视觉样式清理

- 移除 `.layer-group-children::before` 左侧装饰竖线（可能干扰组复选框区域）
- 移除 `.layer-item:has(:checked)` 的 `box-shadow: inset` 绿条（避免被感知为阴影）
- 移除 `.layer-group-arrow` 的 `opacity: 0.7/0.6` 透明度过渡，改为纯色常显

## 组状态指示器重设计

- `.group-status` 从 8px 灰色圆点改为 16px 圆形徽章，带 `textContent` 文字（✓/◐/⏳/✕）
- 各状态配色：idle=灰色描边，loaded=绿色填充+光圈，partial=琥珀色+光圈，loading=脉冲动画，error=红色
- 暗色模式全线适配新配色

## CSS 冗余清理

- geojsonloader.css 移除 `details.layer-section` 及 summary 全套样式（已被 main.css 覆盖）
- geojsonloader.css 移除 `.layer-plain` 冗余样式（主样式在 main.css）
- geojsonloader.css 补充文件头部说明：主样式已迁移至 main.css

## 搜索功能修复

- `buildSearchIndex` 增加 `.catch()` 处理 IDB 缓存异常降级（原代码 IDB 拒绝时 callback 永不触发 → 索引不构建 → 提示卡死）
- `initSearch` 开头增加 `updateSearchInputState()` 调用，同步初始状态
- 搜索输入框增加 `:disabled` CSS 样式（浅色/暗色模式），索引构建时视觉可辨识

## 搜索要素功能再次修复（双重保障）

- 要素搜索第二阶段改为「倒排索引优先 + 全量特征扫描兜底」的双重策略
  - 方案A：原有倒排索引匹配（精确 token + 模糊子串）
  - 方案B：方案A未命中时，直接全量遍历 `si.features` 做 `indexOf` 属性值匹配
  - 确保即使索引构建有问题，要素搜索始终有结果返回
- 修复模糊搜索空 `candidateSets` 时误判为匹配的问题
- 要素搜索第二阶段改为双源策略：优先 `searchIndexMap`，兜底 `featureCache`

## 地图设置：自动识别剪贴板开关

- 地图设置新增「自动识别剪贴板」开关（默认开启），控制 pointdrop.js 的全局粘贴监听和切页自动读取
- 关闭后 `Ctrl+V` 和 `visibilitychange`/`focus` 自动读取均跳过
- 状态持久化到 localStorage `dupal_toggle_clipboard`

## 搜索快捷键

- 全局 `Ctrl+F`（或 `Cmd+F`）聚焦搜索框并全选

## 搜索索引修复（根因查明）

- 根因：UI 初始化时（第2007行）`searchRegistry.push` 提前占位了所有图层的 checkboxId
- `onDataLoaded` 中 `if (!searchRegistry.find(...))` 永远为 false → 搜索索引永不重建
- 修复：改为判断 `if (!searchIndexMap[checkboxId])`，仅当真正有索引数据时才跳过

## 组状态指示器精简

- `.group-status` 从 16px 回退为 10px 纯色圆点，`display: inline-flex` + `min-width/min-height` 确保正圆
- 移除所有 border、box-shadow、文字（✓/◐/⏳/✕），仅用颜色区分状态
- `updateGroupStatus` 完全移除 `textContent` 设置，仅保留 `dataset.status` 和 `title`
- 暗色模式同步精简
- 组状态圆点与图层要素状态联动：`updateLayerItemStatus` 内自动触发 `syncGroupLoadingStatus`

## 要素搜索恢复为「仅限已勾选图层」

- 还原 `cb.checked` 检查：要素搜索第二阶段只搜索当前已勾选的图层
- 用户上传图层同样遵循此逻辑（checkbox 同步检查）
- 以提升搜索相关性和性能（不搜索不可见图层的要素）

## 用户上传图层持久化

- Leaflet.GzIdbLoader.js 新增 `delCache(key)` 和 `deleteSearchIndex(cacheKey)` 方法
- 用户上传图层时：保存 GeoJSON 数据到 IDB（key: `user_geo_<persistentId>`）
- 搜索索引改用 `user_<persistentId>` 作为 cacheKey 写入 IDB（不再传 null 跳过缓存）
- localStorage 维护持久化图层列表 `dupal_user_layers: [{id, fileName}]`
- 页面初始化时调用 `restoreUserLayers()` 从 IDB 恢复所有用户上传图层
- 删除图层时：清理 `layerCache`/`searchIndexMap`/`featureCache`/`searchRegistry` + IDB 缓存 + localStorage 记录
- 恢复时 IDB 缓存丢失自动清理无效的 localStorage 记录

## 底图名称精简

- 图层控件中所有带「底图」后缀的名称均移除冗余字眼：
  - `ArcGIS-影像底图` → `ArcGIS-影像`
  - `ArcGIS-街道底图` → `ArcGIS-街道`
  - `ArcGIS-海洋底图` → `ArcGIS-海洋`
  - `ArcGIS-世界地形基础底图` → `ArcGIS-世界地形`
  - `ArcGIS-地形底图` → `ArcGIS-地形`
  - `天地图影像底图` → `天地图影像`
  - `天地图矢量底图` → `天地图矢量`
  - `天地图地形底图` → `天地图地形`
- 保持 ArcGIS- 前缀以区分来源

## 记住图层开关

- 地图设置新增「记住图层」开关（默认开启）
- 关闭时立即清除所有已保存的 IDB 缓存和 localStorage 记录
- 打开时立即扫描当前内存中的用户图层并保存到 IDB + localStorage
- 开关状态持久化到 localStorage `dupal_toggle_rememberLayer`
- 文字「自动识别剪贴板」简化为「识别粘贴」

## 图层勾选状态持久化

- 用户上传图层每次勾选/取消时保存状态到 `localStorage`（`dupal_user_layer_<persistentId>`）
- 恢复用户图层时读取保存的勾选状态，按原样显示（部分开部分关）
- 内置静态矢量要素图层同样持久化（`dupal_layer_<checkboxId>`）
- `restoreLayerCheckStates()` 在初始化时恢复所有内置图层的勾选状态，自动触发已勾选图层的加载

## 地图设置开关 - 数据驱动重构

- 将 10 个重复的 toggle-bar HTML 块抽象为 `TOGGLE_GROUPS` 数据数组（按分类组织：显示、控件、工具、数据）
- 数据驱动渲染函数替代手写 HTML，后续新增开关只需在对应分类数组中添加一项
- 新增分类标题行 `.toggle-category` CSS，视觉上分组显示，告别平铺清单
- 保持所有 DOM ID 不变，确保已有事件绑定正常工作
- 浅色/暗色模式 CSS 同步适配分类标题

## 拆分 app.js — 版号/导出/清理代码从主脚本中独立

- 从 `index.html` 主脚本块（~1560 行）中拆出约 290 行非地图核心逻辑到 `assets/app.js`：
  - 版本号读取（fetch service-worker.js → CACHE_NAME）
  - 版号点击 → 清理菜单（clearSWCache、clearIDB、doRefresh）
  - 导出地图图片（exportMapImage + Ctrl+E 快捷键）
  - SW 更新弹窗（_showUpdateToast）
- 主脚本块现在只保留地图初始化、控件、图层管理等强相关代码

## 再次迁移 app.js — SW注册 + 剪贴板/记住图层开关

- 将 head 中的 Service Worker 注册代码迁入 `app.js`（与已有的 `_showUpdateToast` 合并）
- 将主脚本中的「自动识别剪贴板」和「记住图层」开关初始化也迁入 `app.js`
- SW 注册改用 `function` 代替箭头函数，与 app.js 风格统一

## 大重构：toggleConfig→app.js + 控件懒加载 + 文件处理→geojsonloader.js

- **togggleConfig + initToggle**（~140 行）移入 `app.js`，与应用管理的主题一致
- **scale / mousePos / Geoman 改为懒加载**：不提前创建控件实例，只在用户打开对应 toggle 时才 `addTo(map)`，节省首屏创建开销
- **文件处理代码**（~540 行，loadFileAsUserLayer / drag-drop / PWA launchQueue）移入 `geojsonloader.js`，紧邻其依赖的 `addUserLayer`
- `rebuildLayerCtrl` 暴露为 `window.rebuildLayerCtrl` 供 app.js 中的 layerCtrlToggle 调用
- 主脚本块从 ~1100 行缩减到 ~500 行，index.html 从 ~1460 行缩减到 820 行
- 迁移后主脚本减少约 85 行，三个功能模块在 app.js 中按「应用管理」主题聚合

## 修复：全选/全不选操作未持久化图层勾选状态

- 根因：`selectAllLayers`、`unselectAllLayers`、组级 select-all handler、用户图层 select-all handler 均直接设置 `cb.checked`，未触发 `change` 事件 → localStorage 从未写入
- 修复：新增 `persistLayerCheckState(cb, checked)` 工具函数，统一处理内置图层 (`dupal_layer_`) 和用户图层 (`dupal_user_layer_`) 的 key 差异
- 在全部 4 个全选/全不选路径中调用该函数
- 用户图层 checkbox 增加 `dataset.persistentId` 属性，供 persistLayerCheckState 查找正确的 localStorage key

## 地图设置开关渲染 + moreBasemap init 移入 app.js，加入 PWA 缓存

- toggle 数据驱动渲染脚本（TOGGLE_GROUPS ~60 行）从 inline 移入 `app.js`
- `moreBasemapToggle` 初始化及事件绑定从主脚本移入 `app.js`
- 主脚本再减 ~80 行，`index.html` 降至 ~740 行
- `service-worker.js` 的 `STATIC_ASSETS` 列表新增 `"./assets/app.js"`

## 修复：app.js 中 toggle 数据驱动渲染执行顺序错误

- 根因：toggle 渲染代码被错误地放在 toggleConfig（initToggle）之后执行，所有 `getElementById` 返回 null，tooggle 开关完全不显示
- 修复：将渲染 + moreBasemap init 调整到 toggleConfig 之前执行，确保 checkbox DOM 在 initToggle 读取之前已创建
- 同时清理了 Python 脚本操作遗留的重复 toggleConfig 代码段和残缺注释

## 修复：懒加载控件默认不显示 + hasControl 报错

- 根因1：懒加载重构后，`initToggle` 初始同步只处理了「未勾选→移除」，忘记处理「已勾选→创建」，鼠标坐标等默认开启的控件全程不出现
- 根因2：`map.hasControl()` 在该 Leaflet 版本中不存在，导致 `zoomToggle` 初始化时抛出 `TypeError`，后续 toggle 全部中断
- 修复1：`initToggle` 中增加 `if (checked) { if (cfg.enable) cfg.enable(); }` 分支
- 修复2：去掉 `hasControl` 前置判断，直接 `cfg.control.addTo(map)`（Leaflet 的 addTo 本身会处理重复添加）
- 修复3：`toggleConfig` 中 `mouseCoordToggle` 调至 `geomenToggle` 之前，确保鼠标坐标优先加载

---

## 终局总结：今日全部重构内容

### 最终文件结构

```text
index.html（698 行，原 ~1460 行）
├── inline: 天地图 TK、PWA install prompt
├── inline: 地图初始化 L.map + 底图图层定义（tdt/arcgis/etopo/osm）
├── inline: getVisibleLayers / rebuildLayerCtrl + basemap 持久化
├── inline: Geoman pm:create 事件 + 测距工具函数
└── inline: loadingOverlay 显示/隐藏

assets/app.js（655 行，今日新建）
├── 版本号显示（fetch service-worker.js → CACHE_NAME）
├── 版号点击 → 清理菜单（clearSWCache / clearIDB）
├── 导出地图图片（exportMapImage + Ctrl+E）
├── SW 更新弹窗（_showUpdateToast）
├── Service Worker 注册
├── 自动识别剪贴板开关初始化
├── 记住图层开关初始化
├── 地图设置开关 - 数据驱动渲染（TOGGLE_GROUPS）
├── moreBasemapToggle 初始化及事件绑定
└── 地图控件开关持久化（toggleConfig + initToggle，含懒加载）

assets/geojsonloader.js（3736 行，追加 ~280 行）
├── 原有的图层管理、搜索、group 交互
├── 追加：loadFileAsUserLayer / deliverToMap（文件解析）
├── 追加：PWA launchQueue 文件打开
├── 追加：drag/drop 拖放支持（含文件夹递归）
└── 追加：persistLayerCheckState（全选/全不选持久化）

service-worker.js（新增 app.js 到缓存列表）

### 点投成功提示改用 dialog.js 弹窗组件

- 删除 pointdrop.js 中自建的轻量 `showToast` 函数（~20 行 inline 样式 div）
- 改用 `window.showToast`（来自 dialog.js 的全局 toast 组件），样式统一、支持动画/操作按钮
- `dialog.js` 移至 `<head>` 中加载，早于所有业务脚本，确保 `showToast/closeToast/showMarkdown` 全局可用
```

### 所有 toggle 开关的归属

| 开关       | init 代码位置             | 控制对象              | 加载策略                  |
| ---------- | ------------------------- | --------------------- | ------------------------- |
| 点要素聚类 | geojsonloader.js          | clusterEnabled 变量   | eager（影响图层渲染）     |
| 显示标签   | geojsonloader.js          | labelEnabled 变量     | eager（影响图层渲染）     |
| 鼠标坐标   | app.js toggleConfig       | mousePosControl       | **懒加载**                |
| 缩放控件   | app.js toggleConfig       | map.zoomControl       | eager（Leaflet 默认创建） |
| 比例尺     | app.js toggleConfig       | scaleControl          | **懒加载**                |
| 图层控件   | app.js toggleConfig       | rebuildLayerCtrl()    | eager（主脚本先创建）     |
| 编辑测量   | app.js toggleConfig       | map.pm.addControls    | **懒加载**                |
| 更多底图   | app.js moreBasemap init   | allBaseLayers 子集    | eager（底图数据已加载）   |
| 识别粘贴   | app.js clipboard init     | pointdrop.js 监听开关 | eager（仅绑定事件）       |
| 记住图层   | app.js rememberLayer init | IDB + localStorage    | eager（仅绑定事件）       |

### 修复的 bug 清单

1. 全选/全不选不持久化 → 新增 `persistLayerCheckState`
2. app.js 顺序错误（渲染在 config 之后）→ 调整到 config 之前
3. 懒加载控件默认不创建（鼠标坐标等不显示）→ initToggle 增加 enable 调用
4. `map.hasControl is not a function` → 去掉 hasControl 检查
5. 控件加载顺序（鼠标坐标应优先于编辑测量）→ 重排 toggleConfig

---

## 最终文件架构

```text
index.html（~700 行）
  主脚本: L.map + 底图定义 + rebuildLayerCtrl + 持久化 + pm:create
  剩余 inline: PWA install prompt, loadingOverlay

assets/
├── geo-utils.js        (405行)  纯函数工具库（颜色/坐标/样式/弹窗）
├── geo-config.js       (117行)  图层路径与分组配置（之前放 geojsonloader 头部）
├── geojsonloader.js    (3366行) 核心：图层加载/样式/高亮/交互/搜索/文件上传
├── file-handler.js     (463行)  外部文件导入（loadFileAsUserLayer/drag-drop/PWA launch）
├── app.js              (~650行) 应用管理（版本号/导出/SW/剪贴板/记住图层/
│                                  toggle 渲染 + toggleConfig + moreBasemap）
├── dialog.js           (401行)  全局弹窗组件（showToast/closeToast/showMarkdown）
├── pointdrop.js        (583行)  投点编辑器
└── 第三方库（leaflet/shp/jszip/togeojson/marked/...）

加载顺序：
  <head>: dialog.js → geo-config.js ← 图层数据，早于所有业务脚本
  <body>: 主脚本 → app.js → geojsonloader.js → file-handler.js
          → marked.min.js → pointdrop.js
```

### 遗留：待进一步拆分（当前在 geojsonloader.js IIFE 内部，需暴露内部状态）

| 模块            | 行数 | 说明                                                                                 |
| --------------- | ---- | ------------------------------------------------------------------------------------ |
| 文件/文件夹上传 | 527  | `handleFileUpload`/`handleFolderUpload`，依赖 `addUserLayer`/`layerCache`            |
| 搜索功能        | 729  | 倒排索引 + tokenize + 搜索 UI，依赖 `searchRegistry`/`searchIndexMap`/`featureCache` |

### 今日全部改动（按时间顺序）

1. 地图设置开关 → 数据驱动 TOGGLE_GROUPS + CSS 分类
2. 创建 app.js ← 从主脚本拆出版本号/导出/SW 更新
3. SW 注册 + 剪贴板/记住图层 → app.js
4. toggleConfig + 懒加载控件 + 文件处理 → app.js + geojsonloader.js
5. toggle 渲染 + moreBasemap → app.js
6. PWA 缓存加入 app.js
7. 全选/全不选持久化修复
8. app.js 执行顺序修复 + 懒加载 init 修复 + hasControl 修复
9. 点投 toast 改用 dialog.js 全局组件
10. dialog.js 移至头部加载
11. 外部文件处理拆为 file-handler.js
12. 图层配置数据拆为 geo-config.js（路径 + geoJsonGroups 数组）
13. 搜索索引构建时增加 toast 提示：⏳ 正在建立索引… → ✅ 索引就绪
14. 面板滚动重构：新增 `.panel-scroll` 容器（地图设置 + 搜索栏 + 图层列表统一滚动），移除 toggle-body 的独立滚动
