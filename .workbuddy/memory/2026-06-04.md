# 2026-06-04

## Leaflet.VectorGrid 测试（test.html）

### 技术路线
- 用 `L.vectorGrid.slicer()` 替代手动三副本 `shiftLng()` + `L.geoJSON`
- VectorGrid 自动处理世界循环（跨 180° 反子午线重复），不需要手动偏移

### 关键发现
- VectorGrid 的 `L.SVG.Tile` 重写了 `onAdd = L.Util.falseFn`，导致 `this._drawing` 从未被设置为 `true`
- `L.SVG._updatePoly()` 开头有 `if (!this._drawing) return;`，因此所有 fill 渲染被跳过（只有 stroke 能看到）
- **修复**：在 `rendererFactory` 里设置 `r._drawing = true`

### 实施代码
```js
rendererFactory: function(tileCoord, tileSize, opts) {
    var r = L.svg.tile(tileCoord, tileSize, opts);
    r._drawing = true;
    return r;
}
```

### 样式配置
- `vectorTileLayerStyles.sliced` 可以是 function(properties, zoom) 返回对象
- 用 `props.FID || props.OBJECTID` × 黄金角 137.508° 做每要素散色
- 必须显式设置 `fill: true, stroke: true`

### 交互事件
- `interactive: true` + `.on("mouseover", fn)` 可用
- `e.layer.properties` 含 GeoJSON 属性（sliced web worker 已把 `tags` 映射为 `properties`）
- `fakeStop` 报错：`L.canvas.tile` 有 `L.DomEvent.fakeStop` 兼容问题（旧版 API），改用 `L.svg.tile` 无此问题

### 备案信息
- test.html 和 index.html 添加公安备案链接：`浙公网安备33030402001567号`
- 备案图标路径：`./assets/images/备案编号图标.png`
- 图标样式：`vertical-align:-2px;height:1em;margin-right:3px;`（-2px 比 middle 更对齐中文字体）
