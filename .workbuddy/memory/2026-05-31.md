# 2026-05-31

## 修改 about 链接打开方式
- 文件：`assets/geojsonloader.js` (第 211 行)
- 修改：在 aboutLink 创建代码中添加 `aboutLink.target = "_blank"`
- 效果：点击感叹号图标打开 about 页面时，现在会在新标签页打开
- 原因：用户要求新页面打开 about 页面

## 侧边栏图钉功能
- 文件：`assets/main.css`、`assets/geojsonloader.js`
- 修改1：`assets/main.css` 末尾添加 `.sidebar-pin-btn` 样式和 `body.sidebar-pinned` 状态样式
- 修改2：`assets/geojsonloader.js` 在 titleRow 中添加图钉按钮（📌），className = `sidebar-pin-btn`
- 修改3：`assets/geojsonloader.js` 添加图钉按钮点击事件，toggle `body.sidebar-pinned` class，状态存入 `localStorage`
- 修改4：`assets/geojsonloader.js` 修改点外部关闭逻辑，钉住时（`sidebar-pinned`）不自动关闭面板
- 实现方式：钉住时面板改为 `position: relative` 进入文档流，`body` 是 flex 容器，`#map` 用 `flex:1` 占满剩余空间；钉住时隐藏 `.layer-trigger`
- 状态记忆：`localStorage` 存储 `yugis_sidebar_pinned`，刷新后自动恢复；钉住/取消时调用 `map.invalidateSize()`
