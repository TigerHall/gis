# 2026-07-20

## 新增「高程读取」插件 + 设置开关
- 新插件 `assets/Leaflet.ElevationQuery.js`（标准 Leaflet 插件，`L.Class`+`L.Evented`）：点击地图查询任意坐标高程/水深。
  - 数据源可配置：`source:'gebco'`（默认，WMS GetFeatureInfo）｜自定义函数 `{query(latlng,ctx)}`｜`{type:'wms',...}`。运行时可 `eq.setSource(...)` 切换（满足"后续换高程源"）。
  - 单例挂载 `map.elevationQuery({source})`；`enable()/disable()/query(latlng)`；事件 `enable/disable/querystart/result/error`。
  - UI：底部居中信息栏 `.elev-query-bar` + 点击点弹窗 `.elev-popup`，复用站点设计语言（绿色描边、圆角、feature-popup 表格式、CSS 变量自动深色）。
- 配套样式 `assets/elevation-query.css`。
- 开关：在 `app.js` 的 `TOGGLE_GROUPS`「高级」分类新增 `elevationReadToggle`（默认关闭），`toggleConfig` 新增对应 `enable/disable`（懒加载 `ensureElevationQuery()` 单例）。
- `index.html` 引入 css（head）与 js（app.js 前）。
- 注意：默认 gebco 源忠实沿用用户样例的 WMS 1.3.0 + CRS=EPSG:4326 + toBBoxString(lon,lat) 参数；若日后发现跨子午线/高纬 I/J 偏移，可改 `version:'1.1.1'`+`SRS` 或自定义源。

## 新增 5 个图层（geo-config.js + geojsonloader.js）
- 全部放入「社会热点专题」分组（用户明确点名 item1 放此组；item2/3 的 Natural Earth 基础地理数据也随入此组，因该组已含 countries-* 国家图层）。
- `浙江适飞区_20260512.geojson.gz`：显示名"浙江适飞区(2026-05-12)"，source=浙江省交通运输厅 2026-05-12 公告正文（无 labelField，自动识别）。
- Natural Earth 4 层（source 均为 https://www.naturalearthdata.com/）：
  - `countries.geojson.gz` "国家行政区 countries" labelField=NAME_ZH → searchPriority:true（默认搜索）
  - `geography_marine_polys.geojson.gz` "海区 geography_marine_polys" labelField=name_zh → searchPriority:true
  - `ports.geojson.gz` "港口 ports" labelField=name → searchPriority:true
  - `states_provinces.geojson.gz` "省级行政区 states_provinces" labelField=name_zh（仅加图层，未进默认搜索）
- 新增图层配置项 `source`：在 geojsonloader.js 加 `layerSourceMap`，UI 构建时填充；`onDataLoaded` 把 `source` 注入每个要素 `properties.数据源`，弹窗/tooltip 展示。已在 geo-config.js 头部文档补 `source` 字段说明。
- searchPriority 语义 = 未勾选也可搜 + 结果置顶（启动 2s 后台静默建索引），即用户说的"默认搜索"。
