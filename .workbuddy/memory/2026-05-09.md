# 2026-05-09

## Leaflet.MousePosition 全屏按钮功能

为 `assets/Leaflet.MousePosition.js` 增加了全屏按钮功能：
- 新增 `showFullscreen: Boolean` 选项（默认 false），启用后在控件最右侧追加全屏按钮
- 新增 `fullscreenTarget: String` 选项（CSS 选择器），指定全屏目标元素，默认 `document.documentElement`
- 使用 Fullscreen API（兼容 webkit/moz/ms 前缀），进入/退出全屏自动切换图标（⛶ / ✕）
- 监听 `fullscreenchange` 事件，Esc 退出时按钮图标同步更新
- CSS 修改：容器改为 `inline-flex`，新增 `.leaflet-mouse-position-text` 文本节点和 `.leaflet-mouse-position-fullscreen` 按钮样式
- `index.html` 中已启用 `showFullscreen: true`

## 点要素标签开关功能

优化点要素展示，完成标签开关功能：
- `Leaflet.GeoMarker.js`：去掉 `minZoom` 参数，`showLabel` 恒为 `!!labelText`，删除所有 `_updateLabelVisibility` 方法
- `geojsonloader.js`：删除 `CLUSTER_MIN_ZOOM`、`NO_CLUSTER_FILES` 常量；删除 `zoomHandler` 及所有引用；新增 `initLabelToggle()` 实现标签开关（localStorage 持久化，CSS 类 `labels-disabled` 控制显隐）
- `index.html`：在聚类开关下方添加「显示标签」开关按钮
- `main.css`：新增 `.labels-disabled .station-label { display: none !important; }`（标签是 divIcon 内的 `<span class="station-label">`，非 Leaflet tooltip）

## leaflet-markers-canvas-clustered.js 重构
- 将 rbush.js v4.0.1 内联，零依赖单文件（15.7KB）
- 修复 bug：features[indices[i]]] -> features[indices[i]]，grid[keys[j]]] -> grid[keys[j]]，hits -> hits，L.DomUtil.addClass 缺逗号
- 美化：单点增加白色描边，聚类圆改为统一颜色 #3388ff
- geojsonloader.js >10K 分支改用 setFeatures()，真正零 DOM 节点
- index.html 删除 rbush.js 独立引用，删除 assets/rbush.js（已内联）
## 2026-05-09 工作记录

### 修复 leaflet-markers-canvas-clustered.js

1. **删除未使用文件**：`assets/Leaflet.CanvasMarkerLayer.js` 和 `assets/leaflet-markers-canvas.js` 未被 `index.html` 加载，已删除。`index.html` 只加载 `leaflet-markers-canvas-clustered.js`，三个文件没必要分开。

2. **添加鼠标悬停指针效果**：
   - `onAdd` 中添加 `map.on("mousemove", this._fire, this)`
   - `onAdd` 中添加 `map.on("mouseout", this._onMouseOut, this)`
   - `onRemove` 中对应解绑
   - 新增 `_onMouseOut` 方法，鼠标离开地图时复位光标
   - 复用现有 `_fire` 方法处理 mousemove 命中检测（已有 `cursor = "pointer"` 逻辑）

3. **增大点尺寸**：参考 IODP 点（DOM 渲染 radius: 8）
   - canvas 单点圆半径：6px → **8px**
   - 命中区域：8px → **10px**

4. **修复 45 万点加载慢问题**：
   - 根因：`removeGeoJSONLayer()` 每次把 `layerCache[checkboxId]` 置 null，导致取消勾选再重新勾选时必须重新 build 全部要素
   - 修复：`removeGeoJSONLayer` 只执行 `map.removeLayer()`，不再清 cache
   - `loadGeoJSONLayer` 命中缓存时直接 `addTo(map)`，无需重建
   - `Leaflet.GzIdbLoader.js` 的 `fetchWithCache` 已正确缓存解压后的 JSON（非 gz），并添加了性能日志

### 修改文件清单
- `assets/leaflet-markers-canvas-clustered.js`：mousemove/mouseout 事件 + 点大小 6→8→10
- `assets/geojsonloader.js`：`removeGeoJSONLayer` 不再清 layerCache
- `assets/Leaflet.GzIdbLoader.js`：`fetchWithCache` 添加性能日志
- 删除 `assets/Leaflet.CanvasMarkerLayer.js`（未使用）
- 删除 `assets/leaflet-markers-canvas.js`（未使用）
